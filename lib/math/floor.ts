/**
 * Computes number rounded down to precision
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/math/floor.ts source}
 *
 * @param num - The number to round down
 * @param precision - The precision to round down to
 * @returns Returns the rounded down number
 *
 * @example
 *
 * ```js
 * floor(4.006) // => 4
 *
 * floor(0.046, 2) // => 0.04
 *
 * floor(4060, -2) // => 4000
 *
 * ```
 */
export default function floor(num: number, precision = 0) {}
