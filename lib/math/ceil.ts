/**
 * Computes number rounded up to precision
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/math/ceil.ts source}
 *
 * @param num - The number to round up
 * @param precision - The precision to round up to
 * @returns Returns the rounded up number
 *
 * @example
 *
 * ```js
 * ceil(4.006) // => 5
 *
 * ceil(6.004, 2) // => 6.01
 *
 * ceil(6040, -2) // => 6100
 *
 * ```
 */
export default function ceil(num: number, precision = 0) {}
