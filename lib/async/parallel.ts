/**
 * Run multiple asynchronous functions in parallel
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/async/parallel.ts source}
 *
 * @param value - The value to check
 * @returns - Returns true if value is NaN, else false
 *
 * @example
 *
 * ```js
 * isNaN(NaN) // => true
 *
 * isNaN(new Number(NaN)) // => true
 *
 * isNaN(undefined) // => false
 *
 * ```
 */
export default function parallel() {}
