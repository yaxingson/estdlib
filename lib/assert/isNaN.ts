/**
 * Checks if value is NaN
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/assert/isNaN.ts source}
 *
 * @param value - The value to check
 * @returns - Returns true if value is NaN, else false
 *
 * @example
 *
 * ```js
 * isNaN(NaN) // => true
 *
 * isNaN(new Number(NaN)) // => true
 *
 * isNaN(undefined) // => false
 *
 * ```
 */
export default function isNaN(value: unknown) {}
