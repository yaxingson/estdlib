/**
 * Round a number to the specified precision
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/number/round.ts source}
 *
 * @param num - The number to be rounded
 * @param precision - The decimal place
 * @returns Returns the number after rounding
 *
 * @example
 *
 * ```js
 * round(1.2345) // => 1
 *
 * round(1.2345, 2) // => 1.23
 *
 * round(1.2345, 3) // => 1.235
 *
 * ```
 */
export default function round(num: number, precision: number = 0) {}
