/**
 * Generates random integer within the specified range
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/number/inRange.ts source}
 *
 * @param minimum - Lower bound of random integer
 * @param maximum - Upper limit of random integer (not included)
 * @returns Returns a random integer in the specified range
 */
export default function randomInt(minimum: number, maximum: number) {}
