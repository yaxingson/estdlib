/**
 * Calculate the average of an array of numbers
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/number/mean.ts source}
 *
 * @param values - The array of numbers to calculate the average
 * @returns Returns average of all the numbers in the array
 *
 * @example
 *
 * ```js
 * const numbers = [1, 2, 3, 4, 5]
 *
 * mean(numbers) // => 3
 *
 * ```
 */
export default function mean(values: number[]) {}
