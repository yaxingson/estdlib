/**
 * Produces a random number between the inclusive lower and upper bounds
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/number/random.ts source}
 *
 * @param lower - The lower bound
 * @param upper - The upper bound
 * @returns Returns the random number
 *
 * @example
 *
 * ```js
 * random(0, 5) // => a number between 0 and 5
 *
 * ```
 */
export default function random(lower: number = 0, upper: number = 1) {}
