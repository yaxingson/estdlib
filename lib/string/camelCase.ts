/**
 * Converts string to camel case
 *
 * @since 0.1.2
 * @see {@link https://github.com/yaxingson/estdlib/blob/main/lib/string/camelCase.ts source}
 *
 * @param str - The string to convert
 * @returns Returns the camel cased string
 *
 * @example
 *
 * ```js
 * camelCase('Foo Bar') // => 'fooBar'
 *
 * camelCase('--foo-bar--') // => 'foobar'
 *
 * camelCase('--FOO-BAR--') // => 'foobar'
 *
 * ```
 */
export default function camelCase(str: string) {}
