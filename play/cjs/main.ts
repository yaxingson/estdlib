#!/usr/bin/env tsx
const estdlib = require('estdlib')
const { head, isArray, now } = require('estdlib')
const assert = require('estdlib/assert')
const { isBoolean, isDate } = require('estdlib/assert')
const math = require('estdlib/math')
const { pick, valuesIn, update, toPairs } = require('estdlib/object')

